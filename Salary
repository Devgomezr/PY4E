# add $1.5 when there is overtime hours (40+)

hrs = input("Enter Hours:")
h = float(hrs)
rate = 10.50
rateOP= 1.5

if h <= 40: 
    print (h * rate)
else: 
    oT = h - 40
    otR = (rate *40) + (rateOP * rate * oT)
    print (otR)
