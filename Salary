# add $1.5 when there is overtime hours (40+)

hrs = input()
h = float(hrs)
rate = float(input())


if h <= 40: 
    print (h * rate)
else: 
    oT = h - 40
    otR = (rate *40) + (1.5 * rate * oT)
    print (otR)
